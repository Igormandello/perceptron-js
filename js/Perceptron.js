class Perceptron {
  constructor(numberOfWeights) {
    this.weights = [];
    this.lr = 0.0001;
    this.bias = Math.random() - 0.5;
  }

  predict(inputs) {
    return 0;
  }

  train(inputDatas, outputs) {
    return;
  }
}